rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Accès réservé aux utilisateurs authentifiés via Firebase Auth
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
